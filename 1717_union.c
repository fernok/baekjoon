#include <stdio.h>
#include <stdlib.h>

struct node{
  int data;
  struct node *left;
  struct node *right;
};

int n, m;
node *arr;

void append(int i, int j)
{
  return;
}

void check(int i, int j)
{
  return;
}

int main(void)
{
  int i, j;
  scanf("%d%d", &n, &m);
  arr = (node*)malloc(sizeof(node) * (n + 1));
  for(i = 1; i <= n; i++)
  {
    arr[i]
  }
  return 0;
}
